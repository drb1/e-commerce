import React from 'react';
import {View} from "react-native";
import SearchBarLayout from "./SearchBar/SearchBarLayout";

const AppBar = () => {
    return (
       <View>
           <SearchBarLayout/>
       </View>
    );
};

export default AppBar;
